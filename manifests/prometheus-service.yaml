apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: monitoring
spec:
  selector:
    app: prometheus
  ports:
    - name: web
      port: 9090              # Service port (inside cluster)
      targetPort: 9090        # Pod container port
      nodePort: 30914         # Fixed NodePort on the node
  type: NodePort
